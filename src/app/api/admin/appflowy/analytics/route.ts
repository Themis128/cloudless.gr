/** * /api/admin/appflowy/analytics — backed by AppFlowy */ import { NextRequest, NextResponse } from "next/server"; import { requireAdmin } from "@/lib/api-auth"; import { listAllWorkspaces, listWorkspaceViews, AppFlowyNotConfiguredError } from "@/lib/appflowy"; interface AnalyticsEvent { id: string; name: string; type: string; count: number; lastEdited: string; } async function getPrimaryWorkspaceId(): Promise<string | null> { const workspaces = await listAllWorkspaces(); return workspaces[0]?.workspace_id ?? null; } export async function GET(request: NextRequest) { const auth = await requireAdmin(request); if (!auth.ok) return auth.response; const { searchParams } = new URL(request.url); const days = Number(searchParams.get("days") ?? 30); try { const workspaceId = await getPrimaryWorkspaceId(); if (!workspaceId) { return NextResponse.json({ error: "No AppFlowy workspace found" }, { status: 404 }); } const views = await listWorkspaceViews(workspaceId); const events: AnalyticsEvent[] = views.slice(0, 100).map((v) => ({ id: v.view_id, name: v.name, type: v.type, count: 1, lastEdited: v.last_edited_time, })); return NextResponse.json({ events, count: events.length, days }); } catch (err) { if (err instanceof AppFlowyNotConfiguredError) { return NextResponse.json({ error: "AppFlowy not configured" }, { status: 404 }); } return NextResponse.json({ error: "Failed to fetch analytics" }, { status: 500 }); } }