/** * Client report email utilities. */ import type { ClientPortal } from "./client-portals"; export function generateClientReportEmail(portal: ClientPortal) { if (!portal.steps) return null; const deliverables = portal.deliverables || []; return { portalName: portal.name, clientName: portal.clientName || "Client", clientEmail: portal.clientEmail, steps: portal.steps, deliverables, paymentLinks: portal.paymentLinks || [], }; }