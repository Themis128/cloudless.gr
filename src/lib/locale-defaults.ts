/**
 * Shared locale & currency defaults used across pricing, emails, and dates.
 * Centralised here so the site stays consistent.
 */
export const DEFAULT_LOCALE = "en-IE";
export const DEFAULT_CURRENCY = "EUR";
