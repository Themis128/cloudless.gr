/** * Instrumentation utilities for observability and monitoring. * This file provides compatibility wrappers for AWS SDK in Cloudflare Workers environment. */ export const logger = { info: (message: string, ...args: unknown[]) => console.log(JSON.stringify({ level: "INFO", message, ...(args.length ? { data: args } : {}) })), warn: (message: string, ...args: unknown[]) => console.warn(JSON.stringify({ level: "WARN", message, ...(args.length ? { data: args } : {}) })), error: (message: string, ...args: unknown[]) => console.error(JSON.stringify({ level: "ERROR", message, ...(args.length ? { data: args } : {}) })), debug: (message: string, ...args: unknown[]) => console.debug(JSON.stringify({ level: "DEBUG", message, ...(args.length ? { data: args } : {}) })), };