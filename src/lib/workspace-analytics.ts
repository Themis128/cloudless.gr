/** * Workspace analytics utilities. */ import type { ClientPortal } from "./client-portals"; export function computeWorkspaceMetrics(portals: ClientPortal[]) { return { totalPortals: portals.length, activePortals: portals.filter((p) => p.reportsEnabled !== false).length, }; } export function getWorkspaceId(portal: ClientPortal): string { return portal.workspaceId || ""; }