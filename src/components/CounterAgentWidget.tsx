"use client";

/**
 * Placeholder — the Cloudflare `agents/react` package is not in this app's
 * dependency tree. Keep the export so any lazy imports resolve without
 * breaking the production build.
 */
export function CounterAgentWidget() {
  return null;
}
