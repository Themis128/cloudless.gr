/**
 * Minimal config to pass TypeScript validation for `next build`
 * The actual Cloudflare config is in open-next.config.cloudflare.ts
 */
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({});
