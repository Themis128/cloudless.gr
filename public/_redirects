# www redirect - canonical domain handling
www.cloudless.gr/:splat cloudless.gr/:splat 301

# API routes - handled by Worker, proxy mode
/api/* https://cloudless.gr/api/:splat 200

# SPA fallback - serve index.html for client-side routing
/* /index.html 200
