# Broken Workflows Fix — Task Tracker

## Failing Workflows Identified:
1. **CI** — `pnpm lint:md` fails: `skills/postiz-hub/SKILL.md:100` missing trailing newline (MD047)
2. **e2e-full-coverage** — 9 test failures: postiz-ai-proxy Worker sends `extra_body` (unsupported by NVIDIA → 400), hits rate limits (503); ai-backend-chain has `typeof` operator precedence bug
3. **LinkedIn poll** — curls down Pi → 502/000
4. **deploy-pi** — Pi down → 502 on health check
5. **HTTPS health probe** — Pi down → Tailscale OAuth identity empty

## Fixes:
- [x] Analyze all 5 failing workflows
- [ ] Fix #1: markdown trailing newline in skills/postiz-hub/SKILL.md
- [ ] Fix #2: postiz-ai-proxy Worker — remove extra_body (NVIDIA rejects it)
- [ ] Fix #3: e2e/ai-backend-chain.spec.ts — typeof precedence bug (typeof body.text ?? ... → typeof (body.text ?? ...))
- [ ] Fix #4: e2e/postiz-ai-proxy.spec.ts — accept 503 as valid (NVIDIA rate limiting)
- [ ] Fix #5: LinkedIn poll — add fallback origin + resilience to Pi being down
- [ ] Fix #6: HTTPS health probe — improve Tailscale OAuth error handling when Pi is down
- [ ] Fix #7: deploy-pi — investigate smoke test for workflow bugs
- [ ] Verify all fixes with actionlint
