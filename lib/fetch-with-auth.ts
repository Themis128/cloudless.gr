/** * Wrapper around fetch that injects the session cookie / auth headers * expected by the backend. Adjust to match your real auth scheme. */ export async function fetchWithAuth(input: RequestInfo | URL, init?: RequestInit) { const sessionId = typeof window !== "undefined" ? localStorage.getItem("sessionId") : null; const headers = new Headers(init?.headers); if (sessionId) { headers.set("x-session-id", sessionId); } return fetch(input, { ...init, headers, credentials: "include", }); }