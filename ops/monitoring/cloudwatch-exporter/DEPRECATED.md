# DEPRECATED — CloudWatch exporter (AWS/SQS)

**Do not apply.** Next.js ISR revalidation no longer uses AWS SQS.
Use in-cluster Prometheus / Grafana on omv.

CloudWatch scrape of `AWS/SQS` is retired (Cloudflare Free preference).
