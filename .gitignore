# Dependencies
node_modules

# Cloudless Design System - local reference kit (per-machine)
Cloudless Design System (1)/

# One-off installer scripts parked here from a Claude session (do not commit)
install-branding.sh
ship-campaign-fixes.sh
recovery.sh
recovery2.sh

# Per-session scratch scripts (do not commit). Anything matching
# `tmp_*.sh` is a Claude-session helper that the agent forgot to delete
# before `git add -A`. Catch them here so they never reach a PR.
tmp_*.sh
tmp_*.mjs

# Amazon Q dev-chat exports
q-dev-chat-*.md

# Lockfiles (pnpm-lock.yaml is the only allowed lockfile)
package-lock.json
yarn.lock
# Exception: ETL workflows use a standalone npm project under scripts/etl/
# and need their package-lock.json committed for `npm ci` to work in CI.
!scripts/etl/package-lock.json

# Next.js
.next
.next-e2e
.open-next
out

# SST
.sst

# Build
dist
build
*.tsbuildinfo

# SST-generated Python type stubs (regenerated on every `sst deploy`)
sst.pyi

# Lighthouse workflow artifacts (per-run outputs — never commit)
lighthouse_report.json
lighthouse-*.json

# Environment variables
.env
.env.*
!.env.example

# Private keys / service-account credentials — never commit
*.pem
*.key
*.p12
*.pfx
*.jks
GOOGLE_PRIVATE_KEY.pem

# OS files
.DS_Store

# Pochi (AI coding agent) — may contain tokens; managed per-machine
.pochi/
# Local AI-stack launcher scripts (per-machine tooling)
scripts/tabby-up.sh
scripts/tabby-pochi-proxy.mjs
# Ad-hoc test runner output dumps
test_output.txt
Thumbs.db

# IDE
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
*.swp
*.swo

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test coverage
coverage

# Playwright
test-results
playwright-report

# Misc
*.log
*.bak
build-output*.txt

# Ignore insiders AI rules
.github/instructions/codacy.instructions.md

# HubSpot config file
hubspot.config.yml

# pnpm store
.pnpm-store/

# Claude Code state — session/project caches stay local, but committed
# agent and skill definitions under .claude/agents/ and .claude/skills/
# are shared with the team. Use .claude/* (not .claude/) so the directory
# itself isn't ignored — otherwise the !.claude/agents/** negation cannot
# re-include children.
.claude/*
!.claude/agents/
!.claude/agents/**
!.claude/skills/
!.claude/skills/**
# cloudless-design is installed per-machine from the design-system kit
# (see Cloudless Design System (1)/HANDOFF.md). Personal scope - re-ignore.
.claude/skills/cloudless-design/
!.claude/commands/
!.claude/commands/**
!.claude/hooks/
!.claude/hooks/**
!.claude/settings.json

# Scratch / ghost files from FUSE mount
test-write.txt
docker-on-windows-eval.html
.devcontainer/Dockerfile2
.devcontainer/test.txt
.github/test.txt
src/test.txt

# Temp files from Cowork / FUSE sandbox sessions
_tmp_*

# Local attachment files and temporary skill artifacts
cloudless-design-system.skill
cloudless-gr-seo-audit.html
hubspot-billing.skill
hubspot-billing/
hubspot-client-management.skill
hubspot-client-management/
hubspot-crm-reporting.skill
hubspot-crm-reporting/
hubspot-deal-pipeline.skill
hubspot-deal-pipeline/
hubspot-lead-management.skill
hubspot-lead-management/
hubspot-support-tickets*
hubspot-skills-review.html

# Local Claude dispatch tooling and eval workspaces
claude-dispatch.skill
claude-dispatch/
claude-dispatch-skill.skill
claude-dispatch-skill/
claude-dispatch-workspace/

# ESPHome secrets — contains Wi-Fi credentials and API keys
infrastructure/esp32-watchdog/esphome/secrets.yaml
# ESPHome OTA flash logs (generated locally, not source)
infrastructure/esp32-watchdog/esphome/ota_log*.txt

# Local Lighthouse / CWV report dumps (generated, not source)
reports/

# Playwright test artifacts
playwright-results.txt
playwright-report/
test-results/

# Stray dir created by Turbopack when NEXT_DIST_DIR uses an absolute path.
# Path-resolution bug in Next.js writes a relative ".//home/..." instead
# of treating the absolute path as absolute, creating a phantom home/ dir.
/home/

# Python — local virtualenv and bytecode caches
.venv/
__pycache__/
*.py[cod]
*.pyo
tmp/
.env.e2e
.env.e2e.local
e2e/.auth/
.playwright-cache/
coverage/
.coverage-cache/
.monocart/
.coverage-v8-server/
.coverage-run/
infrastructure/terraform/.terraform/
infrastructure/terraform/.terraform.lock.hcl
.env.local
scripts/etl/node_modules
# scripts/etl/package-lock.json is COMMITTED — `npm ci` in the ETL
# workflows reads it. Was previously ignored; un-ignored 2026-06-20 after
# the etl-compute-rfm-churn workflow failed with EUSAGE on missing lockfile.

# ESP32 firmware secrets — never commit, copy from .template
infrastructure/esp32-watchdog/arduino/**/secrets.h

# Cleanup 2026-06-21 — keep these out of the repo
kubectl
.env.local.bak-*
recovery*.sh
ship-*.sh
.git-rewrite/
ai-ui/
.agent-memory/
.dev.vars

# ---------------------------------------------------------------------------
# Architecture / Cloudflare re-architecture docs (local planning ledgers)
# Keep out of git — working notes for the AWS→Cloudflare cutover.
# ---------------------------------------------------------------------------
**/ARCHITECTURE*.md
**/*ARCHITECTURE*.md
**/*architecture*.md
**/architecture-*.md
docs/optimal-architecture-assessment.md
docs/re-architecture-*.md
docs/master-todo-list.md
docs/current-source-of-truth-checklist.md
documentation/optimal-architecture-assessment.md
documentation/architecture-*.md
SST-CLOUDFLARE-*.md
FULL-CLOUDFLARE-*.md
DEVCLOUDFLARE-*.md
CLOUDFLARE-*-ARCHITECTURE*.md
CLOUDFLARE-ARCHITECTURE*.md
CLOUDFLARE-DEPLOYMENT-*.md
CLOUDFLARE-DEV-SERVER-*.md
CLOUDFLARE-EDGE-*.md
CLOUDFLARE-FIX-*.md
CLOUDFLARE-SYNC-*.md
CLOUDFLARE-TUNNEL-*.md
AWS-MIGRATION-*.md
MIGRATION-*.md
TODO_MIGRATION.md
START-MIGRATION.md
RUN-MIGRATION.md

# Wrangler / miniflare local state (D1/KV/R2)
.wrangler/

# Local DB snapshots + kubectl port-forward PIDs (SQLTools)
.local/

# Local lint tool caches (yamllint / actionlint)
.tools/
test-results.json/
.lighthouseci/

# gh-aw audit downloads (`gh aw audit <run-id>`)
.github/aw/logs/
audit-links.zip
