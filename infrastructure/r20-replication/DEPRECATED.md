# DEPRECATED — DynamoDB R20 replication Lambda

**Do not deploy.** Auth and app state live in Cloudflare D1 (`user-auth-db`).
There is no DynamoDB replica to maintain.

AWS Lambda / DynamoDB Streams path is retired (Cloudflare Free preference).
