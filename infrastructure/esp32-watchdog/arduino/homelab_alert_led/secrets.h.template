/*
 * secrets.h.template — copy to secrets.h before building.
 *
 * secrets.h is .gitignored — do NOT commit real credentials.
 *
 * Build & flash:
 *   cp secrets.h.template secrets.h
 *   <edit secrets.h, fill in real values>
 *   arduino-cli compile --fqbn esp32:esp32:esp32 .
 *   arduino-cli upload  --fqbn esp32:esp32:esp32 -p /dev/ttyUSB0 .
 *   # OR for OTA (no USB needed), see skills/esphome-ota-flash/SKILL.md
 */
#pragma once

// WiFi
#define WIFI_PASS       "REPLACE_ME_WITH_WIFI_PASSWORD"

// Mosquitto MQTT auth — Phase 3 cutover requires both.
// Username/password are tbaltzakis / <unified admin password> per
// project_unified_admin_creds in operator memory.
#define MQTT_USERNAME   "tbaltzakis"
#define MQTT_PASSWORD   "REPLACE_ME_WITH_UNIFIED_ADMIN_PASSWORD"
