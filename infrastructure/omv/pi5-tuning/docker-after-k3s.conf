# Stagger Docker until k3s control plane is up (Pi 5 boot storm mitigation).
# Unit name on omv is k3s-k3s-omv.service (OMV); keep k3s.service as alias.
[Unit]
After=k3s-k3s-omv.service k3s.service network-online.target
Wants=k3s-k3s-omv.service
