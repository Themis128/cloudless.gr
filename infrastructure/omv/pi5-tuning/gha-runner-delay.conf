# Delay GHA runners so they do not compete with k3s + mailcow at boot.
[Unit]
After=k3s-k3s-omv.service k3s.service docker.service network-online.target
Wants=k3s-k3s-omv.service

[Service]
# Give kubelet ~2 minutes to bring critical pods Ready before CI starts.
ExecStartPre=/bin/sleep 120
