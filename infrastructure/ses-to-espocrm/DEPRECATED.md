# DEPRECATED — AWS Lambda SES → EspoCRM

**Do not deploy.** Inbound mail for EspoCRM uses Cloudflare Email Routing /
`workers/mail-ingest` and the self-hosted webmail path on omv-ha.

AWS SES + Lambda + S3 ingest is retired (Cloudflare Free preference).
