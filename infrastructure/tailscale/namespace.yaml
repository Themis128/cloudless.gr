# Tailscale Operator — obsolete local Secret/RBAC stub.
# Helm chart `tailscale/tailscale-operator` owns the namespace, RBAC, and OAuth Secret.
# Kept so older docs/scripts that `kubectl apply -f namespace.yaml` stay harmless.
apiVersion: v1
kind: Namespace
metadata:
  name: tailscale
  labels:
    app.kubernetes.io/name: tailscale-operator
