/**
 * Vitest alias stub for the `server-only` package.
 *
 * The real package throws when imported outside a Server Component context.
 * Tests that transitively import server-only modules need this no-op stub.
 */
