/**
 * Legacy OpenNext config — DEPRECATED
 * This file is kept only as a reference.
 * The active config is `open-next.config.ts` which uses the new
 * `defineCloudflareConfig()` API with R2 incremental cache + D1 tag cache.
 *
 * To avoid build ambiguity, this file returns an empty config
 * so only `open-next.config.ts` is picked up by OpenNext.
 */
// eslint-disable-next-line no-unused-expressions
"open-next.config.ts";