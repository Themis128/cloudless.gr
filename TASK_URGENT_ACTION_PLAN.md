# URGENT ACTION PLAN - Cloudless.gr Infrastructure Fixes\n## Priority: HIGH - DNS Resolution + Missing Applications + Monitoring\n\n### Current State Analysis (2026-07-26)\n\n#### DNS Configuration\n- ✅ Routes.yaml defines all services (5 services marked pending)\n- ❌ DNS zone has A record pointing to 192.0.2.1 (test IP, not real)\n- ✅ Ingress-rules.yaml has all service configurations ready\n- ✅ Cloudflare Tunnel ID: e977a490-58c5-4fdb-9155-86832e3e636a\n\n#### Missing Applications (status: pending in routes.yaml)\n```\ndocs.cloudless.gr - docs-server - port 30901\nmeili.cloudless.gr - meilisearch - port 30902\nespocrm.cloudless.gr - espocrm - port 30700\nntfy.cloudless.gr - ntfy - port 30080\nkuma.cloudless.gr - uptime-kuma - port 32501\n```\n\n#### Monitoring Stack\n- ✅ namespace.yaml exists (monitoring namespace)\n- ❌ Needs deployment of Prometheus, Grafana, Alertmanager, Loki\n- ✅ All monitoring configs available in infrastructure/monitoring/\n\n#### NVIDIA_API_KEY\n- ❌ Not configured anywhere\n- Required for omv-ai services (vllm-kimi.yaml)\n\n---\n\n## COMPREHENSIVE TODO LIST\n\n### Phase 1: DNS Resolution Fix (CRITICAL - Must do FIRST)\n- [ ] Update DNS zone A record from 192.0.2.1 to omv IP 192.168.1.128\n- [ ] Apply DNS changes via Cloudflare API or Dashboard\n- [ ] Verify DNS propagation with dig/cloudflare lookup\n\n### Phase 2: Deploy Missing Applications (HIGH)\n- [ ] Deploy docs-server (docs.cloudless.gr)\n- [ ] Deploy meilisearch (meili.cloudless.gr)\n- [ ] Deploy espocrm (espocrm.cloudless.gr)\n- [ ] Deploy ntfy (ntfy.cloudless.gr)\n- [ ] Deploy uptime-kuma (kuma.cloudless.gr)\n\n### Phase 3: Deploy Monitoring Stack (HIGH)\n- [ ] Deploy monitoring namespace\n- [ ] Deploy Prometheus Operator (kube-prom-stack)\n- [ ] Deploy Grafana\n- [ ] Deploy Alertmanager\n- [ ] Deploy Loki for logs\n- [ ] Deploy Promtail for log collection\n- [ ] Configure dashboards\n\n### Phase 4: Add NVIDIA_API_KEY (MEDIUM - Do LAST)\n- [ ] Add NVIDIA_API_KEY to omv-ai deployment\n- [ ] Add to k3s secrets or environment\n- [ ] Update vllm-kimi.yaml with secret reference\n\n---\n\n## DETAILED ACTION STEPS\n\n### PHASE 1: DNS RESOLUTION FIX\n\n#### Step 1.1: Update DNS A Record\n```bash\n# Option A: Cloudflare API (recommended)\nexport CLOUDFLARE_API_TOKEN=\"your-api-token\"\nexport CLOUDFLARE_ZONE_ID=\"7025298073d6a5c645a6ad9add0cbf0e\"\n\ncurl -X PUT \"https://api.cloudflare.com/client/v4/zones/${CLOUDFLARE_ZONE_ID}/dns_records/$(grep -A10 '\"name\": \"cloudless.gr\"' infrastructure/cloudflare-tunnels/dns-zone-config.json | grep '\"id\"' | head -1 | cut -d'\"' -f4)\" \\\n  -H \"Authorization: Bearer ${CLOUDFLARE_API_TOKEN}\" \\\n  -H \"Content-Type: application/json\" \\\n  --data '{\n    \"type\": \"A\",\n    \"name\": \"cloudless.gr\",\n    \"content\": \"192.168.1.128\",\n    \"ttl\": 1,\n    \"proxied\": true,\n    \"comment\": \"OMV Node - Primary server\"\n  }'\n\n# Option B: Manual via Cloudflare Dashboard\n# DNS > cloudless.gr > Type: A > Name: @ > Content: 192.168.1.128 > Proxy: ON\n```\n\n#### Step 1.2: Verify DNS Update\n```bash\ndig cloudless.gr +short\ndig docs.cloudless.gr +short\ndig meili.cloudless.gr +short\n```\n\n#### Step 1.3: Update DNS Records for Subdomains\n```bash\n# docs.cloudless.gr -> 192.168.1.128 (NodePort 30901)\n# meili.cloudless.gr -> 192.168.1.128 (NodePort 30902)\n# espocrm.cloudless.gr -> 192.168.1.128 (NodePort 30700)\n# ntfy.cloudless.gr -> 192.168.1.128 (NodePort 30080)\n# kuma.cloudless.gr -> 192.168.1.128 (NodePort 32501)\n```\n\n### PHASE 2: DEPLOY MISSING APPLICATIONS\n\n\n#### Step 2.1: Verify Applications Are Running Locally\n```bash\n# Check if services are listening on their NodePorts\nsudo ss -tulnp | grep -E '30901|30902|30700|30080|32501'\n\n# Check k3s pods\nkubectl get pods -A\n```\n\n#### Step 2.2: Deploy Missing Applications\n\n**docs-server (docs.cloudless.gr)**\n```bash\n# Check if deployment files exist\nls -la infrastructure/docs-server/ 2>/dev/null || echo \"No docs-server directory found\"\n\n# If using Kustomize/Helm\nkubectl apply -k infrastructure/docs-server/ 2>/dev/null || \\\n  kubectl apply -f infrastructure/docs-server/k8s/ 2>/dev/null || \\\n  echo \"Manual deployment required\"\n```\n\n**meilisearch (meili.cloudless.gr)**\n```bash\nkubectl apply -f infrastructure/meilisearch/namespace.yaml 2>/dev/null\nkubectl apply -f infrastructure/meilisearch/deployment.yaml 2>/dev/null\nkubectl apply -f infrastructure/meilisearch/service.yaml 2>/dev/null\n```\n\n**espocrm (espocrm.cloudless.gr)**\n```bash\nkubectl apply -f infrastructure/espocrm/k8s/ 2>/dev/null || \\\n  kubectl apply -f infrastructure/espocrm/k8s/postgres.yaml \\\n  -f infrastructure/espocrm/k8s/espocrm.yaml \\\n  -f infrastructure/espocrm/k8s/ingress.yaml\n```\n\n**ntfy (ntfy.cloudless.gr)**\n```bash\nkubectl apply -f infrastructure/ntfy/namespace.yaml 2>/dev/null\nkubectl apply -f infrastructure/ntfy/deployment.yaml 2>/dev/null\nkubectl apply -f infrastructure/ntfy/service.yaml 2>/dev/null\nkubectl apply -f infrastructure/ntfy/ingress.yaml 2>/dev/null\n```\n\n**uptime-kuma (kuma.cloudless.gr)**\n```bash\nkubectl apply -f infrastructure/uptime-kuma/namespace.yaml 2>/dev/null\nkubectl apply -f infrastructure/uptime-kuma/deployment.yaml 2>/dev/null\nkubectl apply -f infrastructure/uptime-kuma/service.yaml 2>/dev/null\nkubectl apply -f infrastructure/uptime-kuma/ingress.yaml 2>/dev/null\n```\n\n#### Step 2.3: Update routes.yaml Status\n```bash\n# Change status from \"pending\" to \"active\" for deployed services\nsed -i 's/status: pending/status: active/g' infrastructure/cloudflare-tunnels/routes.yaml\n```\n\n#### Step 2.4: Verify Applications via curl\n```bash\ncurl -I https://docs.cloudless.gr 2>/dev/null && echo \"✅ docs-server OK\"\ncurl -I https://meili.cloudless.gr/health 2>/dev/null && echo \"✅ meilisearch OK\"\ncurl -I https://espocrm.cloudless.gr 2>/dev/null && echo \"✅ espocrm OK\"\ncurl -I https://ntfy.cloudless.gr 2>/dev/null && echo \"✅ ntfy OK\"\ncurl -I https://kuma.cloudless.gr 2>/dev/null && echo \"✅ uptime-kuma OK\"\n```\n\n### PHASE 3: DEPLOY MONITORING STACK\n\n\n#### Step 3.1: Apply Monitoring Namespace\n```bash\nkubectl apply -f infrastructure/monitoring/namespace.yaml\n```\n\n#### Step 3.2: Deploy Prometheus Operator\n```bash\n# Using minimal values to reduce resource usage\nhelm upgrade --install kube-prom-stack \\\n  prometheus-community/kube-prometheus-stack \\\n  -n monitoring \\\n  -f infrastructure/monitoring/kube-prom-stack-values-minimal.yaml\n```\n\n#### Step 3.3: Deploy Grafana\n```bash\n# Grafana is included in kube-prom-stack\n# Access: kubectl port-forward -n monitoring svc/grafana 3000:3000\n```\n\n#### Step 3.4: Deploy Loki & Promtail\n```bash\nkubectl apply -f infrastructure/monitoring/loki-deployment.yaml\nkubectl apply -f infrastructure/monitoring/promtail-values.yaml\n```\n\n#### Step 3.5: Configure Dashboards\n```bash\nkubectl apply -f infrastructure/monitoring/dashboards.yaml\n```\n\n#### Step 3.6: Verify Monitoring Stack\n```bash\nkubectl get pods -n monitoring\nkubectl get svc -n monitoring\n```\n\n---\n\n### PHASE 4: ADD NVIDIA_API_KEY (DO LAST)\n\n#### Step 4.1: Create Secret for omv-ai\n```bash\n# Create secret manifest\ncat > /tmp/omv-ai-secret.yaml <<EOF\napiVersion: v1\nkind: Secret\nmetadata:\n  name: nvidia-api-key\n  namespace: omv-ai\ntype: Opaque\nstringData:\n  NVIDIA_API_KEY: \"your-nvidia-api-key-here\"\nEOF\n\n# Apply secret\nkubectl apply -f /tmp/omv-ai-secret.yaml\n```\n\n#### Step 2.2: Update vllm-kimi.yaml\n```bash\n# Add secret reference to deployment\nsed -i '/containers:/a\\        env:\\n        - name: NVIDIA_API_KEY\\n          valueFrom:\\n            secretKeyRef:\\n              name: nvidia-api-key\\n              key: NVIDIA_API_KEY' \\\n  infrastructure/omv-ai/vllm-kimi.yaml\n```\n\n#### Step 4.3: Redeploy omv-ai\n```bash\nkubectl apply -f infrastructure/omv-ai/vllm-kimi.yaml\n```\n\n---\n\n## POST-DEPLOYMENT VERIFICATION\n\n### Verify All Services\n```bash\n# DNS Resolution\necho \"=== DNS Resolution ===\"\nfor host in cloudless.gr docs.cloudless.gr meili.cloudless.gr espocrm.cloudless.gr ntfy.cloudless.gr kuma.cloudless.gr; do\n  echo \"${host}:\"\n  dig +short ${host} | head -1\ndone\n\n# Application Status\necho \"\\n=== Application Status ===\"\nfor url in docs.meili.espocrm.ntfy.kuma; do\n  echo \"${url/cloudless.}:\"\n  curl -I -s https://${url}.cloudless.gr | head -1\ndone\n\n# Monitoring Stack\necho \"\\n=== Monitoring Stack ===\"\nkubectl get pods -n monitoring 2>/dev/null | grep -E 'Running|STATUS'\n```\n\n### Expected Results\n```\n✅ All DNS records resolve to 192.168.1.128\n✅ docs.cloudless.gr - HTTP 200\n✅ meili.cloudless.gr/health - HTTP 200\n✅ espocrm.cloudless.gr - HTTP 200\n✅ ntfy.cloudless.gr - HTTP 200\n✅ kuma.cloudless.gr - HTTP 200\n✅ Monitoring stack: All pods Running\n```\n\n---\n\n## TROUBLESHOOTING\n\n### If Service Fails to Start\n```bash\n# Check pod logs\nkubectl logs -n <namespace> <pod-name> --previous\n\n# Check events\nkubectl get events -A --sort-by='.lastTimestamp'\n\n# Check resource usage\nkubectl top pods -A\n```\n\n### If DNS Not Resolving\n```bash\n# Check Cloudflare DNS\ndig cloudless.gr @1.1.1.1\n\n# Check local cache\nsudo systemd-resolve --flush-caches\n\n# Verify Cloudflare Tunnel\necho \"Tunnel ID: e977a490-58c5-4fdb-9155-86832e3e636a\"\n```\n\n### If Ingress Not Working\n```bash\n# Check cloudflared config\ncat /etc/cloudflared/config.yml | grep -A5 \"hostname:\"\n\n# Restart cloudflared\nsudo systemctl restart cloudflared\n```\n\n---\n\n## COMPLETION CHECKLIST\n\n### DNS Resolution\n- [ ] cloudless.gr resolves to 192.168.1.128\n- [ ] All subdomains resolve correctly\n- [ ] No DNS propagation delays (>5 minutes)\n\n### Missing Applications\n- [ ] docs.cloudless.gr accessible (HTTP 200)\n- [ ] meili.cloudless.gr/health accessible (HTTP 200)\n- [ ] espocrm.cloudless.gr accessible (HTTP 200)\n- [ ] ntfy.cloudless.gr accessible (HTTP 200)\n- [ ] kuma.cloudless.gr accessible (HTTP 200)\n\n### Monitoring Stack\n- [ ] All monitoring pods Running\n- [ ] Grafana accessible\n- [ ] Prometheus accessible\n- [ ] Loki accessible\n- [ ] Dashboards configured\n\n### NVIDIA_API_KEY\n- [ ] Secret created in omv-ai namespace\n- [ ] vllm-kimi pod has NVIDIA_API_KEY\n- [ ] AI services operational\n\n---\n\n## TIME ESTIMATE\n- **DNS Fix**: 5-10 minutes (API + propagation)\n- **Applications**: 15-30 minutes (deployment + verification)\n- **Monitoring**: 20-40 minutes (Helm + resource download)\n- **NVIDIA_API_KEY**: 5 minutes\n- **TOTAL**: 45-90 minutes\n\n## SUCCESS CRITERIA\n✅ All 5 missing applications deployed and accessible\n✅ Monitoring stack operational\n✅ DNS resolves correctly\n✅ NVIDIA_API_KEY configured (optional for now)\n