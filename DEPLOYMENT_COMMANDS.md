# Deployment Commands for Selfhosted Applications\n\n## Overview\nThis document provides step-by-step commands to deploy all selfhosted applications on the omv node (192.168.1.128).\n\n## Prerequisites\n- SSH key authentication configured (completed)\n- Password authentication disabled\n- kubectl configured with k3s.yaml\n- SSD storage mounted at `/srv/dev-disk-by-uuid-fa6231ab-eae7-40ea-a4b6-400f767a89d7`\n\n## Deployment Order\nDeploy applications in the following order for proper dependency management:\n\n### 1. Deploy Namespaces & Secrets\n```bash\n# Deploy cloudless namespace and secrets\necho \"=== Deploying cloudless namespace and secrets ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/cloudless/namespace.yaml\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/cloudless/secrets.yaml\n\n# Deploy monitoring namespace and secrets\necho \"=== Deploying monitoring namespace ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/monitoring/namespace.yaml\n```\n\n### 2. Deploy Monitoring Stack (Prometheus, Grafana, Loki, Alertmanager)\n```bash\necho \"=== Deploying Monitoring Stack ===\"\n\n# Deploy monitoring components in order\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/monitoring/blackbox-exporter.yaml\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/monitoring/loki-deployment.yaml\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/monitoring/promtail-values.yaml\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/monitoring/kube-prom-stack-values.yaml\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/monitoring/dashboards.yaml\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/monitoring/grafana-ingressroute.yaml\n\n# Verify deployment\necho \"=== Monitoring stack deployment complete ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n monitoring\n```\n\n### 3. Deploy Cloudless Application Stack\n```bash\necho \"=== Deploying Cloudless Application Stack ===\"\n\n# Deploy cloudless application components\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/cloudless/namespace.yaml\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/cloudless/secrets.yaml\n\n# Check if cloudless-app deployment exists\nif [ -f \"infrastructure/cloudless/appflowy-complete.yaml\" ]; then\n    sudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/cloudless/appflowy-complete.yaml\nelif [ -d \"infrastructure/cloudless/k8s\" ]; then\n    sudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/cloudless/k8s/\nfi\n\n# Verify deployment\necho \"=== Cloudless application deployment complete ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n cloudless || echo \"Cloudless namespace not found\"\n```\n\n### 4. Deploy EspoCRM (CRM)\n```bash\necho \"=== Deploying EspoCRM ===\"\n\n# Deploy EspoCRM components\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/espocrm/k8s/\n\n# Verify deployment\necho \"=== EspoCRM deployment complete ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n espocrm || echo \"EspoCRM namespace not found\"\n```\n\n### 5. Deploy Postiz (Social Media Management)\n```bash\necho \"=== Deploying Postiz ===\"\n\n# Deploy Postiz Helm chart (if using Helm)\nif [ -d \"infrastructure/postiz/helm\" ]; then\n    helm upgrade --install postiz infrastructure/postiz/helm/ -n postiz --create-namespace\nfi\n\n# OR deploy Postiz Kubernetes manifests\nif [ -d \"infrastructure/postiz/k8s\" ]; then\n    sudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/postiz/k8s/\nfi\n\n# Verify deployment\necho \"=== Postiz deployment complete ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n postiz || echo \"Postiz namespace not found\"\n```\n\n### 6. Deploy AppFlowy (Productivity)\n```bash\necho \"=== Deploying AppFlowy ===\"\n\n# Deploy AppFlowy complete stack\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/appflowy/appflowy-complete.yaml\n\n# Verify deployment\necho \"=== AppFlowy deployment complete ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n appflowy || echo \"AppFlowy namespace not found\"\n```\n\n### 7. Deploy n8n (Workflow Automation)\n```bash\necho \"=== Deploying n8n ===\"\n\n# Deploy n8n Kubernetes manifests\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/n8n/k8s.yaml\n\n# Deploy n8n workflows\nif [ -d \"infrastructure/n8n/workflows\" ]; then\n    # Workflows are typically configured in n8n UI after deployment\n    echo \"Note: Deploy workflows manually via n8n web interface after deployment\"\nfi\n\n# Verify deployment\necho \"=== n8n deployment complete ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n n8n || echo \"n8n namespace not found\"\n```\n\n### 8. Deploy Additional Services (Optional)\n```bash\necho \"=== Deploying Additional Services ===\"\n\n# Deploy uptime-kuma (monitoring)\nif [ -f \"infrastructure/uptime-kuma/k8s.yaml\" ]; then\n    sudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/uptime-kuma/k8s.yaml\nfi\n\n# Deploy meilisearch (search engine)\nif [ -f \"infrastructure/meilisearch/k8s.yaml\" ]; then\n    sudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/meilisearch/k8s.yaml\nfi\n\n# Deploy docs-server (documentation)\nif [ -f \"infrastructure/docs-server/k8s.yaml\" ]; then\n    sudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/docs-server/k8s.yaml\nfi\n\n# Deploy ntfy (notification service)\nif [ -f \"infrastructure/ntfy/k8s.yaml\" ]; then\n    sudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/ntfy/k8s.yaml\nfi\n```\n\n## Verification Commands\n\n### Check All Deployments\n```bash\necho \"=== Checking all deployments ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -A --no-headers | grep -v Completed | grep -v CrashLoopBackOff\n```\n\n### Check Application Status\n```bash\necho \"=== Application Status Summary ===\"\n\n# Cloudless\necho \"Cloudless:\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n cloudless 2>/dev/null || echo \"  cloudless namespace not found\"\n\n# Monitoring\necho \"Monitoring:\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n monitoring 2>/dev/null || echo \"  monitoring namespace not found\"\n\n# EspoCRM\necho \"EspoCRM:\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n espocrm 2>/dev/null || echo \"  espocrm namespace not found\"\n\n# Postiz\necho \"Postiz:\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n postiz 2>/dev/null || echo \"  postiz namespace not found\"\n\n# AppFlowy\necho \"AppFlowy:\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n appflowy 2>/dev/null || echo \"  appflowy namespace not found\"\n\n# n8n\necho \"n8n:\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -n n8n 2>/dev/null || echo \"  n8n namespace not found\"\n```\n\n### Monitor Deployment Progress\n```bash\necho \"=== Monitoring deployment progress (watch mode) ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get pods -A --watch\n```\n\n## Troubleshooting\n\n### Common Issues\n\n#### 1. Permission Denied Errors\n```bash\n# Fix kubeconfig permissions\nsudo chmod 644 /etc/rancher/k3s/k3s.yaml\n```\n\n#### 2. CrashLoopBackOff\n```bash\n# Check pod logs\necho \"=== Checking omv-ai pod logs ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl logs -n omv-ai <pod-name>\n\n# Describe pod events\necho \"=== Checking omv-ai pod events ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl describe -n omv-ai pod <pod-name>\n```\n\n#### 3. Missing Namespaces\n```bash\n# Create namespace if missing\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl create ns <namespace-name>\n```\n\n#### 4. Resource Constraints\n```bash\n# Check node resources\necho \"=== Node resources ===\"\nssh tbaltzakis@192.168.1.128 \"free -h && df -h\"\n```\n\n### Logs and Debugging\n```bash\n# View all pod logs in a namespace\necho \"=== All pod logs in monitoring namespace ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl logs -n monitoring --all-pods --tail=50\n\n# Check events across all namespaces\necho \"=== All Kubernetes events ===\"\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl get events -A --sort-by='.lastTimestamp'\n```\n\n## Post-Deployment Steps\n\n### 1. Configure Cloudflare Tunnel\n```bash\necho \"=== Configuring Cloudflare Tunnel ===\"\n\n# Apply tunnel configurations\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/cloudflare-tunnels/\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/monitoring/grafana-tunnel.yaml\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/monitoring/cloudflared-drift.yaml\n```\n\n### 2. Verify DNS Records\n```bash\n# Check DNS resolution\necho \"=== Checking DNS resolution ===\"\nnslookup grafana.cloudless.gr\ndig +short grafana.cloudless.gr\n```\n\n### 3. Configure Applications\n- **Grafana**: Access via `http://grafana.cloudless.gr` and configure datasources\n- **n8n**: Access via `http://n8n.cloudless.gr` and configure workflows\n- **EspoCRM**: Access via `http://espocrm.cloudless.gr` and complete setup\n- **Postiz**: Access via `http://postiz.cloudless.gr` and configure integrations\n- **AppFlowy**: Access via `http://appflowy.cloudless.gr`\n\n\n### 4. Set Up Monitoring and Alerts\n```bash\necho \"=== Setting up monitoring and alerts ===\"\n\n# Deploy cluster alerts\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/monitoring/cluster-alerts-kuma-secret.yaml\n\n# Deploy watchdog configurations\nsudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml kubectl apply -f infrastructure/monitoring/omv-watchdogs.yaml\n```\n\n## Success Criteria\n- [ ] All applications deployed and running\n- [ ] All pods in \"Running\" state (no CrashLoopBackOff)\n- [ ] Applications accessible via Cloudflare Tunnel\n- [ ] Monitoring stack operational (Prometheus, Grafana, Loki)\n- [ ] SSD storage utilized for application data\n- [ ] Backup strategy configured\n\n## Notes\n- Deploy applications sequentially as listed above\n- Monitor each deployment with `kubectl get pods -A --watch`\n- Check logs for any errors during deployment\n- Configure applications after deployment via their web interfaces\n- Backup configuration before making significant changes