# IAM (legacy)

AWS IAM docs for this project are archived. Prefer:

- GitHub Actions OIDC (`AWS_DEPLOY_ROLE_ARN` where still needed)
- Cloudflare API tokens / Wrangler secrets
- k8s `cloudless-secrets` on the Pi for app runtime

See [`README.md`](README.md).
