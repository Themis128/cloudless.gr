name: Docker Build & Test

on:
  push:
    branches:
      - main
      - develop
      - application
      - platform
    paths:
      - 'Dockerfile*'
      - 'docker-compose*.yml'
      - 'package.json'
      - 'package-lock.json'
      - '.dockerignore'
  pull_request:
    branches:
      - main
      - develop
      - application
      - platform
    paths:
      - 'Dockerfile*'
      - 'docker-compose*.yml'
      - 'package.json'
      - 'package-lock.json'
      - '.dockerignore'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to build for'
        required: false
        default: 'development'
        type: choice
        options:
          - development
          - production
      triggered_by:
        description: 'Workflow that triggered this step'
        required: false
        default: 'manual'
      commit_sha:
        description: 'Commit SHA that triggered the sequence'
        required: false
        default: ''
      workflow_sequence:
        description: 'Workflow sequence step'
        required: false
        default: ''

permissions:
  contents: read
  security-events: write
  actions: read

env:
  NODE_VERSION: '20'
  DOCKER_BUILDKIT: 1

jobs:
  docker-build-test:
    name: Docker Build & Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        dockerfile: [Dockerfile]
        include:
          - dockerfile: Dockerfile
            target: runner
            tag: runner
    steps:
      - name: Show trigger info
        run: |
          echo "🔗 Docker Build & Test"
          echo "======================"
          echo "Triggered by: ${{ github.event.inputs.triggered_by || 'manual' }}"
          echo "Commit SHA: ${{ github.event.inputs.commit_sha || github.sha }}"
          echo "Workflow sequence: ${{ github.event.inputs.workflow_sequence || 'standalone' }}"
          echo "Environment: ${{ github.event.inputs.environment || 'development' }}"
          echo "Workflow run: ${{ github.run_id }}"
          echo ""
          
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check if Dockerfile exists
        id: check-dockerfile
        run: |
          if [ -f "${{ matrix.dockerfile }}" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "✅ ${{ matrix.dockerfile }} exists"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "❌ ${{ matrix.dockerfile }} not found"
          fi

      - name: Set up Docker Buildx
        if: steps.check-dockerfile.outputs.exists == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Get version info
        if: steps.check-dockerfile.outputs.exists == 'true'
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          GIT_COMMIT=$(git rev-parse --short HEAD)
          GIT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
          BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "git_commit=$GIT_COMMIT" >> $GITHUB_OUTPUT
          echo "git_branch=$GIT_BRANCH" >> $GITHUB_OUTPUT
          echo "build_date=$BUILD_DATE" >> $GITHUB_OUTPUT

      - name: Build Docker image
        if: steps.check-dockerfile.outputs.exists == 'true'
        id: build-image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          target: ${{ matrix.target }}
          push: false
          tags: |
            cloudlessgr-app:${{ matrix.tag }}
            cloudlessgr-app:${{ matrix.tag }}-${{ steps.version.outputs.git_commit }}
            cloudlessgr-app:v${{ steps.version.outputs.version }}-${{ matrix.tag }}
          build-args: |
            VERSION=${{ steps.version.outputs.version }}
            GIT_COMMIT=${{ steps.version.outputs.git_commit }}
            GIT_BRANCH=${{ steps.version.outputs.git_branch }}
            BUILD_DATE=${{ steps.version.outputs.build_date }}
            NODE_ENV=${{ matrix.target }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Test Docker image
        if: steps.check-dockerfile.outputs.exists == 'true' && steps.build-image.outcome == 'success'
        run: |
          echo "🧪 Testing Docker image..."

          # Check if image exists
          if ! docker image inspect cloudlessgr-app:${{ matrix.tag }} > /dev/null 2>&1; then
            echo "❌ Docker image cloudlessgr-app:${{ matrix.tag }} not found"
            echo "📋 Available images:"
            docker images | grep cloudlessgr-app || echo "No cloudlessgr-app images found"
            exit 1
          fi

          echo "✅ Docker image found, testing container startup..."

          # Test container startup
          docker run --rm -d --name test-container cloudlessgr-app:${{ matrix.tag }}

          # Wait for container to start
          sleep 10

          # Check if container is running
          if docker ps | grep -q test-container; then
            echo "✅ Container started successfully"
          else
            echo "❌ Container failed to start"
            docker logs test-container
            exit 1
          fi

          # Test health endpoint (if available)
          if [ "${{ matrix.target }}" = "runner" ]; then
            echo "🔍 Testing health endpoint..."
            # Note: This would need the container to expose port 3000
            # docker run --rm --network host cloudlessgr-app:${{ matrix.tag }} curl -f http://localhost:3000/api/health || echo "Health check not available"
          fi

          # Cleanup
          docker stop test-container || true

      - name: Skip build message
        if: steps.check-dockerfile.outputs.exists == 'false'
        run: |
          echo "⏭️ Skipping build for ${{ matrix.dockerfile }} - file not found"

  docker-compose-test:
    name: Docker Compose Test
    runs-on: ubuntu-latest
    needs: docker-build-test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build development environment
        run: |
          echo "🐳 Building development environment..."
          docker compose -f docker-compose.dev.yml build

      - name: Test development environment
        run: |
          echo "🧪 Testing development environment..."

          # Start services
          docker compose -f docker-compose.dev.yml up -d

          # Wait for services to be ready
          sleep 30

          # Check service status
          docker compose -f docker-compose.dev.yml ps

          # Test Redis connection
          docker compose -f docker-compose.dev.yml exec -T redis-dev redis-cli ping

          # Test application health (if available)
          # curl -f http://localhost:3000/api/health || echo "Health check not available"

          # Stop services
          docker compose -f docker-compose.dev.yml down

  security-scan:
    name: Container Security Scan
    runs-on: ubuntu-latest
    needs: docker-build-test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image for scanning
        id: build-image
        run: |
          echo "🔨 Building Docker image for security scanning..."

          # Get version info
          VERSION=$(node -p "require('./package.json').version")
          GIT_COMMIT=$(git rev-parse --short HEAD)
          GIT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
          BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          # Build the runner image
          docker build \
            --target runner \
            --tag cloudlessgr-app:security-scan \
            --build-arg VERSION="$VERSION" \
            --build-arg GIT_COMMIT="$GIT_COMMIT" \
            --build-arg GIT_BRANCH="$GIT_BRANCH" \
            --build-arg BUILD_DATE="$BUILD_DATE" \
            --build-arg NODE_ENV=production \
            -f Dockerfile .

          echo "✅ Built cloudlessgr-app:security-scan for scanning"
          echo "image_to_scan=cloudlessgr-app:security-scan" >> $GITHUB_OUTPUT

      - name: Run Trivy vulnerability scanner
        if: steps.build-image.outputs.image_to_scan != ''
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ steps.build-image.outputs.image_to_scan }}
          format: 'sarif'
          output: 'trivy-results.sarif'

      - name: Upload Trivy scan results to GitHub Security tab
        if: always() && steps.build-image.outputs.image_to_scan != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Security scan complete
        run: |
          echo "✅ Docker security scan completed"
          echo "📋 Results uploaded to GitHub Security tab"

  docker-push:
    name: Push to Registry
    runs-on: ubuntu-latest
    needs: [docker-build-test, docker-compose-test, security-scan]
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/application'
    environment: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Get version info
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          GIT_COMMIT=$(git rev-parse --short HEAD)
          GIT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
          BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "git_commit=$GIT_COMMIT" >> $GITHUB_OUTPUT
          echo "git_branch=$GIT_BRANCH" >> $GITHUB_OUTPUT
          echo "build_date=$BUILD_DATE" >> $GITHUB_OUTPUT

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push production image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          target: runner
          push: true
          tags: |
            ${{ secrets.DOCKER_REGISTRY }}/cloudlessgr-app:latest
            ${{ secrets.DOCKER_REGISTRY }}/cloudlessgr-app:v${{ steps.version.outputs.version }}
            ${{ secrets.DOCKER_REGISTRY }}/cloudlessgr-app:${{ steps.version.outputs.git_commit }}
          build-args: |
            VERSION=${{ steps.version.outputs.version }}
            GIT_COMMIT=${{ steps.version.outputs.git_commit }}
            GIT_BRANCH=${{ steps.version.outputs.git_branch }}
            BUILD_DATE=${{ steps.version.outputs.build_date }}
            NODE_ENV=production
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push development image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.dev
          target: dev
          push: true
          tags: |
            ${{ secrets.DOCKER_REGISTRY }}/cloudlessgr-app:dev
            ${{ secrets.DOCKER_REGISTRY }}/cloudlessgr-app:dev-${{ steps.version.outputs.git_commit }}
          build-args: |
            VERSION=${{ steps.version.outputs.version }}
            GIT_COMMIT=${{ steps.version.outputs.git_commit }}
            GIT_BRANCH=${{ steps.version.outputs.git_branch }}
            BUILD_DATE=${{ steps.version.outputs.build_date }}
            NODE_ENV=development
          cache-from: type=gha
          cache-to: type=gha,mode=max
