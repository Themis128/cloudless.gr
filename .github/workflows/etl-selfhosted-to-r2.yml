name: ETL — self-hosted apps → R2 data lake

# Migrated version using R2 S3-compatible endpoint.
# Set CLOUDFLARE_ACCOUNT_ID and CF_R2_ACCESS_KEY_ID/CF_R2_SECRET_ACCESS_KEY secrets
# to enable R2 target. Otherwise falls back to AWS S3.
#
# Daily sync of AppFlowy + n8n + Postiz into the cloudless_analytics
# Athena database. Companion to etl-espocrm-to-r2.yml. Tokens come from
# SSM Parameter Store via the same AWS_ETL_ROLE_ARN OIDC role used by the
# other ETL workflows. See:
#   - skills/appflowy-operator/SKILL.md
#   - skills/espocrm-operator/SKILL.md
#   - infrastructure/athena/selfhosted.sql for the Athena DDL.

on:
  schedule:
    - cron: "37 3 * * *"   # 03:37 UTC daily; offset so it doesn't collide with espocrm/gsc
  workflow_dispatch:
    inputs:
      apps:
        description: "Comma-separated subset of {appflowy,n8n,postiz}. Default: all."
        required: false
        default: "appflowy,n8n,postiz"

permissions:
  id-token: write
  contents: read

concurrency:
  group: etl-selfhosted-to-r2
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ${{ fromJSON(vars.RUNNER_GENERIC || '"ubuntu-latest"') }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install ETL deps
        working-directory: scripts/etl
        run: npm ci || npm install

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-1

      - name: Hydrate self-hosted-app tokens from SSM
        run: |
          set -e
          export_param() {
            local name=$1
            local value
            value=$(aws ssm get-parameter --name "/cloudless/production/$name" \
              --with-decryption --query Parameter.Value --output text 2>/dev/null || true)
            if [ -n "$value" ] && [ "$value" != "None" ]; then
              echo "$name=$value" >> "$GITHUB_ENV"
              echo "::add-mask::$value"
            fi
          }
          export_param APPFLOWY_API_URL
          export_param APPFLOWY_JWT_SECRET
          export_param N8N_API_URL
          export_param N8N_API_KEY
          export_param POSTIZ_API_URL
          export_param POSTIZ_API_KEY

      # AppFlowy ETL is postgres-direct (REST /admin/* returned 0 rows on
      # the AC version we run). Needs tailscale + kubectl to reach the
      # in-cluster postgres pod. Pattern matches cluster-doctor.yml.
      - name: Tailscale (for AppFlowy postgres exec)
        if: contains(github.event.inputs.apps || 'appflowy,n8n,postiz', 'appflowy')
        uses: tailscale/github-action@v4
        with:
          authkey: ${{ secrets.TS_AUTHKEY }}
          version: latest

      - name: Set up kubectl with cluster KUBECONFIG
        if: contains(github.event.inputs.apps || 'appflowy,n8n,postiz', 'appflowy')
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG_B64 }}" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config
          kubectl version --client --output=yaml | head -5

      - name: AppFlowy → R2 (postgres-direct via kubectl exec)
        if: contains(github.event.inputs.apps || 'appflowy,n8n,postiz', 'appflowy')
        run: node scripts/etl/appflowy-to-r2.mjs
        continue-on-error: true

      - name: n8n → R2
        if: contains(github.event.inputs.apps || 'appflowy,n8n,postiz', 'n8n')
        run: node scripts/etl/n8n-to-r2.mjs
        continue-on-error: true

      - name: Postiz → R2
        if: contains(github.event.inputs.apps || 'appflowy,n8n,postiz', 'postiz')
        run: node scripts/etl/postiz-to-r2.mjs
        continue-on-error: true