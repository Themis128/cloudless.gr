name: r20-replication-subscriber (RETIRED)

# R20 logical-replication subscriber Lambda is retired. D1 is the source of
# truth; the infrastructure/r20-replication/ directory is deprecated.
# Cloudflare Free only — do not deploy new AWS Lambda code.

on:
  workflow_dispatch:
    inputs:
      note:
        description: Ignored — workflow retired
        required: false
        default: ''

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
    - name: R20 replication subscriber retired
      run: |
        echo "::error::r20-replication-subscriber is retired. D1 is the source of truth; see infrastructure/r20-replication/DEPRECATED.md."
        exit 1
